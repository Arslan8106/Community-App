<h1>New Group</h1>

<%= form_with(model: @group) do |form| %>
  <%=render(NewGroupFormComponent.new(form: form, group: @group ))%>
<%end%>
<br>
<div>
  <%= link_to "Back to articles", groups_path %>
</div>
