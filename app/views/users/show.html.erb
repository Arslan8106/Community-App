<h2 class="text-2xl font-bold leading-7 text-center text-teal-900 sm:text-3xl sm:tracking-tight sm:truncate capitalize"><%= @user.name %>'s Profile</h2>
<div class="relative max-w-md mx-auto md:max-w-2xl min-w-0 break-words bg-stone-200 w-full mb-6 shadow-lg rounded-xl mt-20">
  <div class="px-6">
    <div class="flex flex-wrap justify-center">
      <div class="w-full flex justify-center">
        <div class="relative">
          <% if @user.image.attached? %>
            <%= image_tag @user.image,style: 'height:120px;width:130px;', class:"shadow-xl rounded-full align-middle border-none absolute -m-16 -ml-20 lg:-ml-16 max-w-[150px]"%>
          <%else%>
          <% end %>
        </div>
      </div>
      <div class="w-full text-center mt-20">
        <div class="flex justify-center lg:pt-4 pt-8 pb-0">
          <div class="p-3 text-center">
            <span class="text-xl font-bold block uppercase tracking-wide text-slate-700">3,360</span>
            <span class="text-sm text-slate-400">Photos</span>
          </div>
          <div class="p-3 text-center">
            <span class="text-xl font-bold block uppercase tracking-wide text-slate-700">2,454</span>
            <span class="text-sm text-slate-400">Followers</span>
          </div>
          <div class="p-3 text-center">
            <span class="text-xl font-bold block uppercase tracking-wide text-slate-700">564</span>
            <span class="text-sm text-slate-400">Following</span>
          </div>
        </div>
      </div>
    </div>
    <div class="text-center mt-2">
      <h3 class="text-2xl text-slate-700 font-bold leading-normal mb-1 capitalize"><%= @user.name %></h3>
      <div class="text-xs mt-0 mb-2 text-slate-400 font-bold uppercase">
        <i class="fas fa-map-marker-alt mr-2 text-slate-400 opacity-75"></i><%= @user.email%>
      </div>
    </div>
    <div class="mt-6 py-6 border-t border-slate-200 text-center">
      <div class="flex flex-wrap justify-center">
        <div class="w-full px-4">
          <p class="font-light leading-relaxed text-slate-600 mb-4">An artist of considerable range, Mike is the name taken by Melbourne-raised, Brooklyn-based Nick Murphy writes, performs and records all of his own music, giving it a warm.</p>
          <br>
          <% if user_signed_in? && current_user == @user%>
            <button class="bg-teal-600 hover:bg-teal-900 text-white font-bold py-2 px-4 border-white rounded-lg transition transform hover:-translate-y-0.5">
              <%= link_to 'Edit Profile', edit_user_registration_path(current_user)%>
            </button>
          <%end%>
        </div>
      </div>
    </div>
  </div>
</div>
<br>
<br>
